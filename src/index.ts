// tslint:disable-next-line:no-unused-variable
import Vue from 'vue';

// tslint:disable-next-line:no-unused-variable
import { install, Config } from './install';
import connect from './connect';

const VueMobx = {
    install,
    connect,
}

export default VueMobx;
